[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "greeceapt"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4",
    "playwright",
    "lxml",
]

[tool.setuptools.packages.find]
where = ["src"]
